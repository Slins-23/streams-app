import 'dart:ui';

const Color gray = Color(0xFFD8D8D8); // Used for settings text.
const Color primaryBackground = Color(0xFF33333D); // Background and primary color.
const Color inputBackground = Color(0xFF26282F); // Used for sliders' inactive color, input backgrounds, etc.
const Color cardBackground = Color(0x10FEFEFE); // Used for sliders.